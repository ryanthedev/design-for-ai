# Review: Phase 1 — Relocate doctrine → references/

## Executed Results (Step 0)

This phase is a structural/markdown refactor — no test suite, no typecheck, no linter. Verification is done entirely through the shell commands specified in the DW items. All commands run from `/Users/r/repos/design-for-ai/.claude/worktrees/doctrine-overhaul`.

- Test suite: N/A (no automated tests; docs-only structural refactor)
- Typecheck: N/A
- Lint: N/A
- Shell verification: all DW-specified commands ran and produced the observed outputs recorded below

---

## Requirement Fulfillment

### DW-1.1

PREMISE:  `references/visual/` contains exactly 21 files; `scripts/palette.mjs` exists; `node scripts/palette.mjs --seed 250 --scheme light` exits 0.

EVIDENCE:
```
$ ls references/visual | wc -l
21

$ ls references/visual/
ai-tells.md                    appendix-fonts-and-typography.md  archetypes.md
chapter-01-why-design-matters.md  chapter-02-purpose-of-design.md
chapter-03-typography.md       chapter-04-technology-and-culture.md
chapter-05-proportions.md      chapter-06-composition.md
chapter-07-visual-hierarchy.md chapter-08-color-science.md
chapter-09-color-theory.md     checklists.md  design-dna.md
foundations.md  interaction.md  libraries.md  motion.md
responsive.md  surfaces.md  techniques.md

$ test -e scripts/palette.mjs && echo "palette.mjs exists"
palette.mjs exists

$ node scripts/palette.mjs --seed 250 --scheme light; echo "exit=$?"
/* Generated by design-for-ai palette.mjs */
:root { --neutral-1: #fcfdfd; ... }
/* Contrast report (WCAG 2.x) ... all PASS */
exit=0
```

TRACE: `ls references/visual | wc -l` → 21 files confirmed. `test -e scripts/palette.mjs` → file present. `node scripts/palette.mjs --seed 250 --scheme light` → generates full CSS token block with WCAG contrast report, exits 0.

VERDICT: PASS

---

### DW-1.2

PREMISE:  each of the 6 pillars (content-design, behavioral, journey, deceptive-patterns, design-systems, ai-native) exists at `references/<pillar>/<pillar>.md` with NO YAML frontmatter (first line is not `---`), plus its former reference files present.

EVIDENCE:
```
$ for p in content-design behavioral journey deceptive-patterns design-systems ai-native; do
    FIRST=$(head -1 "references/$p/$p.md" 2>/dev/null)
    echo "$p: |$FIRST|"
  done
content-design:    |The words ARE the interface — not labels placed on it...|
behavioral:        |Behavioral design is the practice of applying...|
journey:           |Journey design is the practice of specifying...|
deceptive-patterns:|Deceptive patterns are design choices that use...|
design-systems:    |A design system turns a one-off design DNA...|
ai-native:         |AI-native design specifies interfaces...|

$ for p in content-design behavioral journey deceptive-patterns design-systems ai-native; do
    echo "=== $p ==="; find "references/$p/" -type f | sort
  done
=== content-design ===
references/content-design/content-design.md
references/content-design/references/content-principles.md
references/content-design/references/microcopy-patterns.md
=== behavioral ===
references/behavioral/behavioral.md
references/behavioral/references/behavioral-principles.md
references/behavioral/references/ethical-application.md
=== journey ===
references/journey/journey.md
references/journey/references/journey-caveats.md
references/journey/references/journey-stack.md
=== deceptive-patterns ===
references/deceptive-patterns/deceptive-patterns.md
references/deceptive-patterns/references/dark-patterns.md
references/deceptive-patterns/references/honest-alternatives.md
=== design-systems ===
references/design-systems/design-systems.md
references/design-systems/references/design-systems-foundations.md
references/design-systems/references/design-systems-governance.md
=== ai-native ===
references/ai-native/ai-native.md
references/ai-native/references/ai-native-caveats.md
references/ai-native/references/ai-native-principles.md
```

TRACE: All 6 `references/<pillar>/<pillar>.md` files exist. Every first line is prose (not `---`), so no YAML frontmatter is present. Each pillar directory contains exactly 2 former reference files in a `references/` subdirectory, confirming the former reference files are present.

VERDICT: PASS

---

### DW-1.3

PREMISE:  `ls skills/ | grep -v -- -workspace` lists exactly: clarify, data-viz, prototype, usability — no `design-for-ai` core dir, none of the 6 pillar dirs.

EVIDENCE:
```
$ ls skills/ | grep -v -- -workspace
clarify/
data-viz/
prototype/
usability/
```

TRACE: The four surviving skills (clarify, data-viz, prototype, usability) are present. `design-for-ai` core directory is absent. None of the 6 pillar dirs (content-design, behavioral, journey, deceptive-patterns, design-systems, ai-native) appear. Exactly 4 entries.

VERDICT: PASS

---

### DW-1.4

PREMISE:  `grep -rn '${CLAUDE_SKILL_DIR}' references/` returns nothing; `grep -rn 'skill directory' references/` returns nothing; spot-check that relocated doctrine files do not link to moved paths that no longer exist (resolve a sample of `${CLAUDE_PLUGIN_ROOT}/...` and relative links).

EVIDENCE:
```
$ grep -rn '${CLAUDE_SKILL_DIR}' references/
(exit code 1 — no output)

$ grep -rn 'skill directory' references/
(exit code 1 — no output)

$ grep -rn '\${CLAUDE_PLUGIN_ROOT}' references/
references/visual/design-dna.md:102:...(`${CLAUDE_PLUGIN_ROOT}/scripts/palette.mjs`)...
references/deceptive-patterns/deceptive-patterns.md:3:...`${CLAUDE_PLUGIN_ROOT}/references/visual/ai-tells.md`...
references/deceptive-patterns/references/honest-alternatives.md:3:...`${CLAUDE_PLUGIN_ROOT}/references/behavioral/`...
references/deceptive-patterns/references/dark-patterns.md:5:...`${CLAUDE_PLUGIN_ROOT}/references/visual/ai-tells.md`...

Path resolution checks:
$ test -e scripts/palette.mjs && echo OK
OK
$ test -e references/visual/ai-tells.md && echo OK
OK
$ test -d references/behavioral/ && echo OK
OK
$ test -e skills/usability/references/usability-principles.md && echo OK
OK
```

TRACE: `${CLAUDE_SKILL_DIR}` — zero matches. `skill directory` — zero matches. All four `${CLAUDE_PLUGIN_ROOT}/...` references in the relocated files resolve: `scripts/palette.mjs` (exists), `references/visual/ai-tells.md` (exists), `references/behavioral/` directory (exists). The `skills/usability/references/usability-principles.md` path (a non-`${CLAUDE_PLUGIN_ROOT}` relative reference in `honest-alternatives.md`) also resolves — usability remains a skill.

VERDICT: PASS

---

## Test-DW Coverage

This phase has no automated test suite — it is a structural markdown/file-organization refactor. All 4 DW items specify shell commands as their explicit verification mechanism. Each item has been verified with recorded observed behavior (the shell command + output is captured in each DW section above). No DW item is testable by automated test; the shell-command verification is the appropriate form.

- [x] DW-1.1 — observed behavior: file count, existence check, script exit code
- [x] DW-1.2 — observed behavior: head -1 per pillar, find per pillar
- [x] DW-1.3 — observed behavior: ls skills/ | grep -v
- [x] DW-1.4 — observed behavior: grep returns nothing; path resolution checks

---

## Dead Code

None found. This phase moves and reorganizes markdown files; there is no code to produce dead branches or unreachable statements.

---

## Correctness Dimensions

| Dimension | Status | Evidence |
|-----------|--------|----------|
| Concurrency | N/A | No concurrent processes; static file reorganization |
| Error Handling | N/A | No I/O paths or external calls in the artifacts |
| Resources | N/A | No file handles, connections, or caches |
| Boundaries | N/A | No collections, strings, or numerics in executable code |
| Security | N/A | No untrusted input; no executable surface in relocated files |

---

## Loaded-Skill Criteria (cc-refactoring-guidance)

| Skill | Criterion | Status | Evidence |
|-------|-----------|--------|----------|
| cc-refactoring-guidance | Fix first, then refactor (separate commits with a boundary) | N/A | This is a pure structural move — no bug fix mixed in; nothing to separate |
| cc-refactoring-guidance | Small changes need MORE rigor (1–5 lines highest error rate) | N/A | This is a large-scale structural move (6 pillar dirs + core dir), not a 1–5 line change |
| cc-refactoring-guidance | Behavior-preserving change to working code | PASS | All 6 pillar body files have their content intact (first lines are substantive prose, former reference files confirmed present at 2 per pillar); `palette.mjs` script behavior unchanged (exit=0, same output structure) |
| cc-refactoring-guidance | No mixed fix-and-refactor in the same commit | N/A | Cannot assess commit history from inside the worktree; the phase is structural reorganization only — no observable bug-fix content in the relocated files |

---

## Edge Case — Dangling Pillar Paths

Checked all `${CLAUDE_PLUGIN_ROOT}/...` references in the relocated files:

| Reference | Resolves to | Exists? |
|-----------|-------------|---------|
| `${CLAUDE_PLUGIN_ROOT}/scripts/palette.mjs` | `scripts/palette.mjs` | YES |
| `${CLAUDE_PLUGIN_ROOT}/references/visual/ai-tells.md` | `references/visual/ai-tells.md` | YES |
| `${CLAUDE_PLUGIN_ROOT}/references/behavioral/` | `references/behavioral/` | YES (dir) |

Also checked for old `skills/behavioral`, `skills/content-design`, `skills/journey`, `skills/deceptive-patterns`, `skills/design-systems`, `skills/ai-native` path references in all `references/` files — zero hits.

The `references/skill-authoring-template.md` contains instructional text like `skills/<pillar>/SKILL.md` (a placeholder showing how to create a new skill), not a pointer to an existing file — this is prose, not a broken link.

VERDICT: PASS — no dangling paths found.

---

## Notes (non-blocking)

1. The CLAUDE.md in the worktree still describes the OLD structure (listing `skills/content-design`, `skills/behavioral`, etc. in its Structure diagram). This is a stale documentation artifact — the actual filesystem matches the new layout, but CLAUDE.md has not been updated to reflect it. Not a DW item; noted here as the natural follow-up task for this phase.

2. `references/deceptive-patterns/deceptive-patterns.md:58` contains a relative path display `references/dark-patterns.md` in a table cell. From that file's location (`references/deceptive-patterns/deceptive-patterns.md`), that relative path resolves to `references/deceptive-patterns/references/dark-patterns.md` which exists. Not a broken link.

---

## Issues

None.

**Verdict: PASS**
